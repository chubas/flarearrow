(* Change actual parameters here, and make sure you change the
   name of this file without the ".template" *)

let db_user       = Some "user";;
let db_pwd        = Some "password";;
let db_host       = Some "host";; (* Use None for default *)
let db_port       = Some 123456;; (* Use None for default *)
let db_dbname     = Some "database";;

let template_dir  = "path/to/views";;
